{% extends 'base.html.twig' %}

{% block title %}Événements de mes villes | La Vie en Rose{% endblock %}

{% block body %}
    <div class="page page--feed">
        <div class="page__container">
            <div class="page__header">
                <h1 class="page__title">Événements dans mes villes</h1>
                <p class="page__subtitle">Les prochains événements dans les villes que vous suivez</p>
            </div>

            {% if events is empty %}
                <div class="empty-state">
                    <div class="empty-state__icon">📍</div>
                    <h2 class="empty-state__title">Aucun événement</h2>
                    <p class="empty-state__text">
                        Aucun événement à venir dans les villes que vous suivez.
                    </p>
                    <div class="empty-state__actions">
                        <a href="{{ path('app_event_index') }}" class="btn btn--primary">
                            Découvrir des événements
                        </a>
                        <a href="{{ path('app_my_follows') }}" class="btn btn--outline">
                            Gérer mes villes
                        </a>
                    </div>
                </div>
            {% else %}
                <p class="page__results">{{ events|length }} événement{{ events|length > 1 ? 's' : '' }} à venir</p>

                <div class="events-grid">
                    {% for event in events %}
                        {% include 'event/_card.html.twig' with {'event': event} %}
                    {% endfor %}
                </div>
            {% endif %}

            <div class="page__back">
                <a href="{{ path('app_profile') }}" class="btn btn--outline">← Retour au tableau de bord</a>
            </div>
        </div>
    </div>
{% endblock %}
